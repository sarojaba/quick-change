import * as React from 'react'

import 'web-toolkit/lib/index.css'

/**
 * @see https://github.com/romgrk/web-toolkit#installation
 */
export function StyleLoader({ theme, children }) {
  return children
}