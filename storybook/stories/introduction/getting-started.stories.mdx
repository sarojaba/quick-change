import { Meta } from '@storybook/addon-docs'

<Meta
  title="Introduction/Getting Started"
/>

# Quick Change

API for Design Systems

## Introduction

퀵체인지는 하나의 API로 다수의 디자인 시스템들을 이용할 수 있도록 도와주는 라이브러리입니다.

이 스토리북은 퀵체인지를 이용해 다양한 디자인 시스템들의 컴포넌트들을 보여주는 것을 목표로 합니다.

## Using Storybook

`T` 키를 눌러 툴바를 표시하고, 디자인 시스템을 바꿔가며 구경해보세요.

> 디자인 시스템간의 CSS 간섭으로 스타일이 원본과 일치하지 않을 수 있습니다. 실제 사용 시에는 꼭 필요한 디자인 시스템만 로드해주세요.

## Using Library

### Installation

코어 모듈과 원하는 디자인 시스템을 설치하세요.

```bash
npm install @quick-change/core
npm install @quick-change/antd @quick-change/bootstrap
```

#### Usage

아래처럼 코드를 작성하세요.

```jsx
import { DesignSystemContext } from '@quick-change/core'

import * as Antd from '@quick-change/antd'
import * as Bootstrap from '@quick-change/bootstrap'

const catalog = {
  'Ant Design': Antd,
  'Bootstrap': Bootstrap
}

<DesignSystemContext catalog={catalog} name="Ant Design">
  <Button>Hello World!</Button>
</DesignSystemContext>
```
