import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { Form, Image, Heading, Input, Button, Paragraph, Checkbox } from '@quick-change/core'

<Meta
  title="Examples/Signin"
/>

# Sign-in

Reference: [Sign-in example](https://getbootstrap.com/docs/5.1/examples/sign-in/)

<Canvas>
  <Story name="Signin">
    <Form>
      <Heading level={1}>Please sign in</Heading>
      <Input
        name="email"
        label="Email address"
        type="email"
        placeholder="name@example.com"
      />
      <Input
        name="password"
        label="Password"
        type="password"
        placeholder="Password"
      />
      <Checkbox name="remember" label="Remember me" />
      <Button name="sign in" type="primary" size="large" block>Sign in</Button>
      <Paragraph>&copy; 2017–2021</Paragraph>
    </Form>
  </Story>
</Canvas>