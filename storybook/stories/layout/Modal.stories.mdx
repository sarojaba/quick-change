import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { Modal, Button } from '@quick-change/core'

import { ModalButton } from './ModalButton'

<Meta
  title="Layout/Modal"
  component={Modal}
/>

export const Template = (args) => <Modal {...args} />

# Modal

[Gallery](https://component.gallery/components/modal/)

Other names: Dialog, Popup, Modal window

A modal is an interface element that appears over other content. It requires an interaction from the user before they can return to whatever is underneath.

<Canvas isColumn>
  <Story name="Modal">
    <ModalButton
      renderButton={
        open => <Button onClick={open}>Click</Button>
      }
      renderModal={
        isOpen => <Modal title="Title" open={isOpen}>Modal body text goes here.</Modal>
      }
    />
  </Story>
</Canvas>

## Close Button

<Canvas isColumn>
  <Story name="Close Button">
    <ModalButton
      renderButton={
        open => <Button onClick={open}>Click</Button>
      }
      renderModal={
        isOpen => <Modal title="Title" open={isOpen} close>Modal body text goes here.</Modal>
      }
    />
  </Story>
</Canvas>

## Properties

<ArgsTable />